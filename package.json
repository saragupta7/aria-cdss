{
  "name": "aria-cdss",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "dev:be": "pnpm --filter backend dev",
    "dev:fe": "pnpm --filter frontend dev",
    "dev:ml": "cd packages/ml-service && uvicorn app:app --reload --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
